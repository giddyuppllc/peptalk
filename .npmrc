legacy-peer-deps=true
# pnpm must hoist node_modules or Expo autolinking + Nitro codegen fail to wire
# react-native-iap's native module on a clean EAS build → "undefined is not a
# function" paywall crash. Local dev can mask this; EAS uses a fresh install.
node-linker=hoisted
